#include "Command.hpp"
using namespace server;

Command::Command (int num, int id, int x, int y, int x2, int y2, int unit, int element, string message) : num(num), id(id), x(x), y(y), x2(x2), y2(y2), unit(unit), element(element), message(message) {

}
