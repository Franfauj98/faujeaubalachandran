#include "MapTest.h"

using namespace std;
using namespace state;

void mapTest(){
  cout << "-------------------------------------" << endl;
  cout << "TEST MAP" << endl;
  cout << "-------------------------------------" << endl;

  // Position p(1,2);
  // Position p2(3,4);
  //
  // Map map;
  // map.addElement(unique_ptr<Element> (new Arrow(2,p2,2)));
  // map.addElement(unique_ptr<Element> (new Arrow(2,p2,2)));
  // map.addElement(unique_ptr<Element> (new Arrow(2,p2,2)));
  //
  // std::vector<std::unique_ptr<Element>> const& myMap = map.getMap();
  //
  // cout << "Taille de map : " << myMap.size() << endl;
  // cout << "Position de son premier élément : (" << myMap[0]->getPosition().getX() << "," << myMap[0]->getPosition().getY() << ")" << endl;
  //
  // cout << "Attributs de l'unité en deuxième position : " << endl;
  // cout << "Life : " << myMap[1]->getLife() << endl;
  // // cout << "Damage : " << myMap[1]->getDamage() << endl;
  // // cout << "StrikeRange : " << myMap[1]->getStrikeRange() << endl;
  // // cout << "MovingRange : " << myMap[1]->getMovingRange() << endl;
  // // cout << "IdUnits : " << myMap[1]->getIdUnits() << endl;
  // // cout << "IdTextureUnits : " << myMap[1]->getIdTextureUnits() << endl;
  // cout << "Position : (" << myMap[1]->getPosition().getX() << "," << myMap[1]->getPosition().getY() << ")" << endl;
  // // cout << "UnitCost : gold : " << myMap[1]->getUnitCost().getGold() << ", food : " << myMap[1]->getUnitCost().getFood() << ")" << endl;
  // map.deleteElement(2);
  //
  // int positionElt = map.getPositionElement(unique_ptr<Element> (new Arrow(2,p2,2)));
  // cout << positionElt << endl;
}
