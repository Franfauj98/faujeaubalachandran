#include "RandomAI.h"
#include "../engine.h"
#include "../state.h"
#include <iostream>
using namespace ai;
using namespace engine;
using namespace state;

RandomAI::RandomAI (){}

void RandomAI::run (engine::Engine& engine, Observable& principalMap, int& counter, bool& canPlay, int id){
  if(canPlay){
    std::vector<unique_ptr<Element>>
    for()


    counter++;
  }

}
